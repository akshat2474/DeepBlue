import 'package:flutter/material.dart';

class AppColors {
  // Exact Grok background color
  static const Color backgroundBlack = Color(0xFF16171A);
  
  // Make buttons and input box the SAME color as Grok
  static const Color cardBackground = Color(0xFF2A2D31);    // Same color for both buttons and input
  static const Color buttonBackground = Color(0xFF2A2D31);  // Identical to cardBackground
  static const Color borderColor = Color(0xFF404448);       // Subtle border
  
  // Text colors
  static const Color primaryWhite = Color(0xFFF0F6FC);
  static const Color primaryBlack = Color(0xFF16171A);
  static const Color greyText = Color(0xFF8B949E);          // Lighter grey for better contrast
  static const Color starColor = Color(0xFFF0F6FC);
}
